{
  "version": 2,
  "routes": [
    { "handle": "filesystem" },

    // これらのパスは常に SPA エントリ（index.html）へ
    { "src": "/(setup|frame)(?:/.*)?", "dest": "/index.html" },

    // /api/ を除くすべてを index.html にフォールバック
    { "src": "/(?!api/).*", "dest": "/index.html" }
  ]
}
